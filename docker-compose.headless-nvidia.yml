services:
  eewrendererstg:
    image: ghcr.io/eewbot/eew-renderer:dev@sha256:f4bd5c871298a820b6fc3ace463ea5667b14649712e1cde49fdc95dc73b71201
    hostname: eewrenderer
    container_name: eewrendererstg${VARIANT:-}__eewrendererstg
    restart: unless-stopped

    environment:
      LISTEN: 0.0.0.0:3000
      ALLOW_DEMO: ${ALLOW_DEMO:-false}
      HMAC_KEY: ${HMAC_KEY}
      INSTANCE_NAME: ${INSTANCE_NAME}
      MINIMUM_RESPONSE_INTERVAL: ${MINIMUM_RESPONSE_INTERVAL:-200ms}
      HEADLESS: true
      IMAGE_CACHE_CAPACITY: ${IMAGE_CACHE_CAPACITY:-512}
      EGL_DEVICE_INDEX: ${EGL_DEVICE_INDEX:-0}

    command: /usr/bin/eew-renderer

    deploy:
      resources:
        limits:
          memory: 2g
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [graphics]

    networks:
      - default

  cloudflared:
    image: cloudflare/cloudflared:2026.8.2
    hostname: cloudflared
    container_name: eewrendererstg${VARIANT:-}__cloudflared

    restart: unless-stopped

    environment:
      TUNNEL_LOGFILE: /dev/stdout

    command: tunnel run --token ${TUNNEL_TOKEN}
    user: root

    deploy:
      resources:
        limits:
          memory: 128m

    networks:
      - default

networks:
  default:
    name: eewrendererstg${VARIANT:-}__default
